# Client UAT ledger

This ledger is the release-facing evidence index for client requirements 4.1–4.11. Automated domain tests remain necessary, but a capability is not marked client-complete until its authenticated journey passes.

| Requirement | Journey | Automated evidence | Current release state |
| --- | --- | --- | --- |
| 4.1 | Calendar and resource lanes | `UAT-4.1`, `P3-02 closed loop`, `FilamentWorkspacePagesTest`, `AllocationConflictTest` | Staff browser journey persists exact-room allocation and verifies the confirmed stay in the hub and calendar; performance profiling remains production-readiness work |
| 4.2 | Guides and operational resources | `UAT-4.1`, `ResourceSuggestionTest`, `GenericOperationalFlowsTest` | Domain-complete; allocation workbench follow-on |
| 4.3 | Availability-first reservation and repeat guests | `UAT-4.3`, `N2 guarded amendment…`, `P3-02 closed loop`, `ClientBookingCoreTest`, `ReservationChangesTest`, `N2ApiIdempotencyTest`, `FilamentResourcesTest` | Browser create → priced snapshot → confirm → exact-room assignment → hub/calendar persistence passes; repeat-guest/companion enhancement remains non-blocking follow-on scope |
| 4.4 | Email and internal notifications | `CommunicationDeliveryTest`, `OutboxAutomationTest` | Local domain-complete; production provider deferred |
| 4.5 | Deposit and payment collection | `UAT-4.5`, `N2 guarded amendment…`, `P3-02 closed loop`, `PaymentEvidenceReviewTest`, `GuestPortalTest`, `ReservationChangesTest`, `CommercialWorkflowTest`, `PaymentRequestLifecycleTest`, `MercadoPagoGatewayContractTest`, `ProviderClosureTest`, `PostgresFinancialConcurrencyTest`, `p3-06a-provider-payments.spec.ts` | Bank transfer remains browser-proven. P3-06A has a real Colombia/MCO COP 10,000 Checkout Pro approval; a dashboard/test notification signed with Mercado Pago's documented HMAC format, submitted through temporary public HTTPS, followed by authoritative lookup; exact-once payment/deposit/folio posting; duplicate/refresh proof; a COP 2,000 seller-UI partial refund completed through authoritative recovery; settlement evidence; and downloaded payment/refund receipts. Colombia/MCO + COP is accepted for this merge. Argentina/MLA + ARS is deferred regional certification, and provider-origin delivery through the final public HTTPS endpoint remains production-activation/final-certification work. Front-desk tender and integrated Point/QR remain P3-06B/C. |
| 4.6 | Finance projections | `UAT-4.1`, `P3-03 generates and opens private PDF, CSV, XLSX, and guest artifacts`, `FinanceReportingTest`, `ReportExportFlowTest`, `DocumentExportApiTest` | Finance requests a filtered revenue XLSX and staff requests an arrivals CSV through the real queued UI, downloads both private artifacts, and opens/parses them; all eight tenant-scoped report definitions have executable column/date/filter coverage |
| 4.7 | Kitchen and dietary planning | `UAT-4.1`, `FilamentKitchenDashboardTest` | Authenticated browser gate passed |
| 4.8 | Tasks and checklists | `UAT-4.8 through 4.10`, `P3-02 closed loop`, `GenericOperationalFlowsTest` | Operations creates and completes a reservation task and marks the assigned room inspected in the state-changing browser journey |
| 4.9 | Extras and final folio | `UAT-4.8 through 4.10`, `P3-02 closed loop`, `P3-03 generates and opens private PDF, CSV, XLSX, and guest artifacts`, `DocumentGenerationFlowTest`, `DocumentExportApiTest`, `N2 guarded amendment…`, `CancellationTemporalCorrectnessTest`, `ReservationChangesTest`, `N2ApiIdempotencyTest`, `PostgresFinancialConcurrencyTest`, `CommercialWorkflowTest`, `GuestPortalWebTest` | Check-in → extra → checkout → manual settlement → zero balance → folio close passes; queued confirmation/itinerary/folio/payment/refund/waiver snapshot/render paths, private integrity-checked download, replacement lineage, and guest phone download are implemented |
| 4.10 | Post-stay survey | `UAT-4.8 through 4.10`, `P3-02 closed loop`, `GuestPortalWebTest`, `FilamentSurveyResponseTrackerTest` | Early checkout dispatches a fresh secure invitation; phone response, duplicate prevention, and authorized staff visibility pass |
| 4.11 | Role boundaries | `P3-02 closed loop`, `RoleSemanticsTest`, `TenantIsolationTest`, `PaymentEvidenceReviewTest`, `ReservationChangesTest` | Storage-isolated Finance, Operations, and Viewer browser sessions pass; Viewer finance denial, cross-property denial, one-time replay, and expired-link denial are exercised |

The final P3-06A gates report SQLite 325 tests / 310 passed / 15 expected PostgreSQL-only skips / 2,396 assertions and PostgreSQL 325 tests / 324 passed / 1 expected host-path skip / 2,433 assertions. Authenticated Playwright passes 7/7 with one separately gated provider test intentionally skipped; public Playwright passes 4/4; the explicit provider Compose journey passes 1/1; GitHub CI passes 8/8. `make test-api-postgres` uses the isolated `inn_test` database for production-engine constraints, uniqueness, and row-lock races.

P3-06A is a merge-accepted test-mode closure. On 2026-08-19, a Colombia/MCO test seller and buyer completed the real COP journey described above. Duplicate delivery plus three browser refreshes left exactly one payment, one paid deposit, and one folio payment line. Authoritative refund lookup completed exactly one Inn refund and one refund folio line. Both immutable receipt PDFs downloaded through authenticated endpoints and rendered cleanly. Provider IDs are retained only in redacted form; no credential, access token, signature, webhook secret, or public-link token is retained in Git.

The MCO account's pending/rejected-card simulations and direct refund API were provider-policy limited; those connection-specific limitations are not current merge blockers. Argentina/MLA + ARS stays explicitly deferred. Production activation still requires a Mercado Pago-originated signed delivery through the final public HTTPS endpoint and real merchant report/payout/withholding reconciliation.

Earlier pre-connection browser evidence on 2026-08-18 used a USD request with no current USD→ARS conversion and therefore rendered a truthful disabled state. That historical run separately proved 390×844 layout, rotation invalidation, revocation, and no plaintext token retention; it is not the later successful MCO/COP provider journey.
